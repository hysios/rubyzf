<%= form_for(@blogor) do |f| %>
  <% if @blogor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@blogor.errors.count, "error") %> prohibited this blogor from being saved:</h2>

      <ul>
      <% @blogor.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
    <div class="field">
      <%= f.label :file %><br />
      <%= f.file_field :file %>
    </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
